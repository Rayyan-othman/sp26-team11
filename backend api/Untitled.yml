info:
  name: Untitled
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/admin/users
  body:
    type: json
    data: |-
      {
        "name": "Test User",
        "email": "user123@test.com",
        "role": "CUSTOMER",
        "accountStatus": "ACTIVE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
